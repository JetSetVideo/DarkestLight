# DarkestLight
A RTS strategy god game

## Dev

Requires Deno 2 (installed on this machine) and a browser.

```bash
deno task dev
```

Then open the printed local URL (default `http://localhost:5173`).

